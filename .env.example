# ============================================
# CONFIGURAÇÕES DE SEGURANÇA (OBRIGATÓRIAS)
# ============================================

# SECRET_KEY: Chave secreta do Django
# GERE UMA NOVA COM: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-MUDE-ESTA-CHAVE-EM-PRODUCAO

# DEBUG: SEMPRE False em produção!
DEBUG=True

# ALLOWED_HOSTS: Hosts/domínios permitidos (separados por vírgula)
# Exemplo produção: ALLOWED_HOSTS=seu-dominio.com,www.seu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1,[::1]

# CSRF_EXTRA_ORIGINS: Origens extras confiáveis para CSRF (separadas por vírgula)
# Exemplo: CSRF_EXTRA_ORIGINS=https://abc.ngrok.io,https://seu-app.pythonanywhere.com
CSRF_EXTRA_ORIGINS=

# ============================================
# DATABASE (Opcional - SQLite por padrão)
# ============================================

# PostgreSQL Database (Recomendado para produção)
DB_NAME=financa_db
DB_USER=postgres
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432

# Para usar SQLite (desenvolvimento), comente PostgreSQL acima e use:
# DB_ENGINE=sqlite3

# ============================================
# OPENAI API
# ============================================

# OPENAI_API_KEY: Chave da API OpenAI (obtenha em https://platform.openai.com/api-keys)
OPENAI_API_KEY=

# Modelos (opcional - valores padrão já definidos)
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_TRANSCRIPTION_MODEL=whisper-1
OPENAI_CHAT_MAX_HISTORY=8

# Email / SMTP (opcional)
# Exemplo usando SMTP (Gmail):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-ou-app-password
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=seu-email@gmail.com
